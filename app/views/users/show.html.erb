



<div id="profile" class="container">
  <div class="userRow1 row">
  	<div class="col s7">
  		<br><br><br>
  		<h2><strong>PROFILE</strong></h2>
  		<br>
  		NAME : <%= @user.name %><br>
  		USER EMAIL :	<%= @user.email %><br>

      <h2><strong>INSTRUCTIONS</strong></h2>
      <ol>
        <li>
          Click 'CREATE A PORTFOLIO' to create a portfolio.
        </li>
        <li>
          Once you created a Portfolio, you can start adding stocks into them.
        </li>
        <li>
          You will have to search the company by ticker(example: yahoo = yhoo, tesla = tsla, google = googl)
        </li>
        <li>
          After you added few stocks into your portfolio, you can browse each stocks and current newsfeed regarding it.
        </li>
        <li>
          To browse a single company that you have added into your portfolio, simply click 'view'.
        </li>
        <li>
          When you browse a single data, you can use the chart to see the company's historical data.
        </li>
      </ol>

  		<br>
  		<br>
  		<br>
  		<% if current_user.id == @user.id %>
  		<%= link_to 'EDIT PROFILE',edit_user_registration_path %> | <%= link_to "Create a Portfolio", new_portfolio_url %>
  			<% end %>
  	</div>
  	<div class="col s5">
      <br><br><br>
  		<%=  image_tag @user.image.url, :class => 'userimg'%>
  	</div>
  </div>



  <div class="userRow1 row">
    <ul class="collection with-header">
      <li class="collection-header"><h4><%= @user.name %>'s Portfolios</h4></li>
      <% @portfolios.each do |entry| %>
      <li class="collection-item"> <%= entry.title %> | <%= link_to ("VIEW"), portfolio_path(entry.id) %> <br> <%= entry.updated_at %><br>
      <%= link_to 'Destroy',  { action: :destroy, id: entry.id }, method: :delete %> <br><br></li>
      <% end %>
    </ul>
  </div>
</div>
